/**
 * Global setup - runs once before all tests
 * This is for database initialization or other one-time setup
 */

export default async function setup() {
  // 可以在这里设置全局测试环境
  // 例如：启动内存数据库、设置环境变量等
  console.log('🧪 Test environment initialized');
}
